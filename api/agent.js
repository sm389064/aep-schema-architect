// Phase 2 stub — AEP Schema Architect Agent
export default function handler(req, res) {
  res.status(501).json({
    error: "Not implemented",
    message: "Phase 2 agent endpoint — not yet implemented.",
    phase: 2
  });
}
